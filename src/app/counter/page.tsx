import React from "react";
import Counter from "./Counter";

export const metadata={
    title:"counter"
}

function page() {
  return (
    <div>
      <Counter/>
    </div>
  );
}

export default page;
