import React from "react";

function page() {
  return <div>Dashboard</div>;
}

export default page;
//we add this default pages to handle refresh while one slot or more didn't match the url because or one other slot change it
